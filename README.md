# segment-tree-rs

Segment tree with point updates, range queries (sum/min/max/gcd), and lazy propagation.

## Features

- `SegTree` — iterative bottom-up segment tree for point updates and range queries
- `LazySegTree` — recursive segment tree with lazy propagation for range-add + range-sum/min/max queries
- Zero dependencies, pure `std`
